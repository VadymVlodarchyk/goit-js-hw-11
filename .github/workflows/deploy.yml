name: Build and deploy to GitHub Pages

on:
  push:
    branches: [main]  # Запуск деплою при пуші в гілку main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1  # Перевірка коду з репозиторію

      - name: Install and build 🔧
        run: |
          npm install  # Встановлення залежностей
          npm run build  # Запуск зборки проекту

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@4.1.0
        with:
          branch: main  # Вказуємо, що деплой буде на гілку main
          folder: dist  # Вказуємо директорію з готовими файлами
